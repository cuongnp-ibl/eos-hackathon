import { connect } from 'react-redux'
import KYBRegistrationForm from '../components/KYBRegistrationForm'

const mapDispatchToProps = (dispatch, props) => ({})

const mapStateToProps = state => ({})

export default connect(mapStateToProps, mapDispatchToProps)(KYBRegistrationForm)
